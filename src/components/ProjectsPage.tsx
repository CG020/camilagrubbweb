import NavBar from "./NavBar";

const ProjectsPage = () => {

    return(
        <div>
            <NavBar message={'Some my work so far . . .'}/>
        </div>
    );
}

export default ProjectsPage;