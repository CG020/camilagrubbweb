import NavBar from "./NavBar";

const ProjectsPage = () => {

    return(
        <div>
            <NavBar message={'Check out some my work so far . . .'}/>
        </div>
    );
}

export default ProjectsPage;